# Spring Data JPA

## 요구사항
- [x] JPA를 활용하여 데이터베이스에 접근하도록 수정한다.
- [ ] 로그인한 사용자의 예약 목록을 조회하는 API를 구현한다.
- [ ] 예약 대기 요청/취소 기능을 구현한다.
- [ ] 내 예약 목록 조회 시 예약 대기 목록도 함께 포함한다.
- [ ] 중복 예약이 불가능 하도록 구현한다.
- [ ] 내 예약 목록의 예약 대기 상태에 몇 번째 대기인지도 함께 표시한다.

## 엔티티 매핑

- [x] `Member`, `Theme`, `Time`을 엔티티로 매핑한다.
- [x] `Reservation`은 `Member`, `Theme`, `Time`과의 연관관계를 매핑한다.

## 기능 목록

### 내 예약 목록 조회

- [ ] `GET /reservations-mine` 요청 시 로그인한 회원의 예약 목록을 반환한다.
- [ ] 내 예약 목록에는 예약과 예약 대기를 함께 포함한다.

### 예약 대기 생성

- [ ] `POST /waitings` 요청 시 예약 대기를 생성한다.
- [ ] 예약 대기 생성 시 로그인한 회원의 정보를 사용한다.

### 예약 대기 취소

- [ ] `DELETE /waitings/{id}` 요청 시 내 예약 대기를 취소한다.

### 중복 방지

- [ ] 같은 날짜, 시간, 테마에 대한 중복 예약 또는 중복 대기는 허용하지 않는다.

## 테스트 기준

### 내 예약 목록 조회

- [ ] 유효한 `token` 쿠키와 함께 `GET /reservations-mine` 요청 시 상태 코드 `200`을 반환한다.
- [ ] 로그인한 회원의 예약과 예약 대기만 조회된다.
- [ ] 응답의 각 항목은 `reservationId`, `theme`, `date`, `time`, `status`를 포함한다.
- [ ] 예약 대기 항목의 상태에는 대기 순번이 포함된다.
- [ ] 내 예약 목록이 없을 경우 빈 리스트를 반환한다.
- [ ] 로그인하지 않은 사용자의 요청은 상태 코드 `401`을 반환한다.

### 예약 대기

- [ ] 유효한 `token` 쿠키와 함께 `POST /waitings` 요청 시 상태 코드 `201`을 반환한다.
- [ ] 유효한 `token` 쿠키와 함께 `DELETE /waitings/{id}` 요청 시 상태 코드 `204`를 반환한다.
- [ ] 중복 예약 또는 중복 대기 요청은 허용하지 않는다.

# Spring MVC [인증]

## 요구사항

- [x] 로그인 기능을 구현한다.
- [x] 로그인 성공 시 세션 id를 `token` 쿠키에 담아 응답한다.
- [x] 쿠키를 이용하여 로그인한 사용자의 정보를 조회한다.
- [x] 쿠키의 인증 정보로 회원 정보를 조회하는 로직을 분리한다.
- [x] `HandlerMethodArgumentResolver`를 이용하여 로그인 회원 정보를 컨트롤러에 주입한다.
- [x] 예약 생성 시 이름이 없으면 로그인한 회원의 정보로 예약을 생성한다.
- [x] 관리자 권한이 있는 회원만 어드민 페이지에 접근할 수 있다.
- [x] `HandlerInterceptor`를 이용하여 권한을 확인하고, 권한이 없으면 상태 코드 `401`을 반환한다.

## 기능 목록

### 로그인

- [x] `POST /login` 요청으로 이메일과 비밀번호를 전달받는다.
- [x] 이메일과 비밀번호가 비어 있거나 공백만 포함되면 상태 코드 `400`과 검증 메시지를 반환한다.
- [x] 이메일 형식이 올바르지 않으면 상태 코드 `400`과 검증 메시지를 반환한다.
- [x] 이메일 또는 비밀번호가 일치하지 않으면 상태 코드 `401`과 동일한 로그인 실패 메시지를 반환한다.
- [x] 세션 id를 `token`이라는 이름의 쿠키에 담아 응답한다.
- [x] 로그인 실패 시 화면에서 오류 메시지를 alert로 표시하고 페이지를 이동하지 않는다.

### 인증 정보 조회

- [x] `GET /login/check` 요청으로 로그인한 사용자의 정보를 조회한다.
- [x] 요청의 `token` 쿠키에 담긴 세션 id로 기존 세션을 조회한다.
- [x] 세션이 없거나 세션에 회원 id가 없으면 상태 코드 `401`과 로그인 필요 메시지를 반환한다.
- [x] 조회한 회원을 응답 DTO로 변환하여 `name` 필드에 이름을 반환한다.
- [x] 인증 정보 조회 API는 주입받은 로그인 회원 정보를 이용하여 이름을 응답한다.

### 로그인 상태 표시

- [x] 로그인 후 상단바 우측의 Login 버튼이 사용자 이름으로 표시된다.

### 예약 생성

- [x] `POST /reservations` 요청으로 날짜, 시간대 id, 테마 id를 전달받는다.
- [x] `ReservationRequest`에 `name`이 있으면 해당 이름으로 회원을 조회하여 예약을 생성한다.
- [x] `ReservationRequest`에 `name`을 생략하거나 `null`로 전달하면 로그인 회원 id로 회원을 조회하여 예약을 생성한다.
- [x] 요청에 `name`이 있으면 로그인한 회원의 이름보다 요청의 이름을 우선하여 예약자를 결정한다.
- [x] 응답의 예약자 이름은 실제 저장된 예약자 이름을 사용한다.
- [x] 로그인하지 않은 사용자의 예약 생성 요청은 상태 코드 `401`을 반환한다.

### 어드민 페이지 접근

- [x] 컨트롤러에 진입하기 전에 `HandlerInterceptor`에서 쿠키의 인증 정보로 회원과 권한을 확인한다.
- [x] 회원의 권한이 `ADMIN`이면 어드민 페이지 접근을 허용한다.
- [x] 현재 관리자 페이지(`/admin`, `/admin/reservation`, `/admin/theme`, `/admin/time`)에 `@AdminOnly`를 적용한다.
- [x] 관리자 페이지 메서드에 `@AdminOnly`를 적용하고, 인터셉터에서 애노테이션을 확인한다.
- [x] 로그인한 회원이 없으면 상태 코드 `401`과 로그인 필요 메시지를 반환한다.
- [x] `ADMIN` 권한이 없으면 상태 코드 `401`과 관리자 권한 필요 메시지를 반환한다.

### 관리자 API 접근

- [x] 시간대와 테마의 생성·삭제 API에 `@AdminOnly`를 적용한다.
- [x] 시간대와 테마 조회 API는 로그인하지 않아도 이용할 수 있다.

## 테스트 기준

### 로그인 화면

- [x] `GET /login` 요청 시 상태 코드 `200`과 로그인 HTML을 반환한다.

### 로그인

- [x] 올바른 이메일과 비밀번호로 `POST /login` 요청 시 상태 코드 `200`을 반환한다.
- [x] 응답의 `Set-Cookie` 헤더에 `token` 쿠키가 포함된다.

### 인증 정보 조회

- [x] 로그인 응답으로 받은 `token` 쿠키를 포함하여 `GET /login/check` 요청 시 상태 코드 `200`을 반환한다.
- [x] 응답의 `name` 필드에 로그인한 회원의 이름을 반환한다.

### 예약 생성

- [x] 유효한 `token` 쿠키와 함께 `name` 없이 예약 생성 요청 시 상태 코드 `201`을 반환한다.
- [x] `name` 없이 생성한 예약의 응답에는 로그인한 회원의 이름을 반환한다.
- [x] 유효한 `token` 쿠키와 함께 `name`을 포함하여 예약 생성 요청 시 상태 코드 `201`을 반환한다.
- [x] 이름을 지정하면 로그인 회원보다 지정한 이름을 우선한다.
- [x] 날짜, 테마 id, 시간대 id가 `null`이면 상태 코드 `400`을 반환한다.
- [x] 로그인하지 않고 예약 생성 요청 시 상태 코드 `401`을 반환한다.

### 어드민 페이지 접근

- [x] `ADMIN` 권한이 없는 회원이 `GET /admin` 요청 시 상태 코드 `401`을 반환한다.
- [x] `ADMIN` 권한이 있는 회원이 `GET /admin` 요청 시 상태 코드 `200`을 반환한다.
- [x] 로그인하지 않은 상태로 `GET /admin` 요청 시 상태 코드 `401`을 반환한다.
- [x] 관리자 하위 페이지에도 동일한 권한 검사가 적용된다.
- [x] 로그인 페이지는 미로그인 상태에서도 상태 코드 `200`과 HTML을 반환한다.

### 관리자 API 접근

- [x] 일반 회원과 미로그인 사용자가 시간대·테마 생성 또는 삭제 요청 시 상태 코드 `401`을 반환한다.
- [x] 관리자의 시간대·테마 생성 요청은 `201`, 삭제 요청은 `204`를 반환한다.
- [x] 미로그인 사용자의 시간대·테마 조회 요청은 상태 코드 `200`을 반환한다.
- [x] 미로그인은 로그인 필요 오류, 일반 회원의 관리자 API 접근은 관리자 권한 필요 오류의 코드와 메시지를 반환한다.
